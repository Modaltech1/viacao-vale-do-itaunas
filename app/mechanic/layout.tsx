import { MechanicShell } from '@/components/layout/admin-shell'

export default function MechanicLayout({ children }: { children: React.ReactNode }) {
  return <MechanicShell>{children}</MechanicShell>
}
